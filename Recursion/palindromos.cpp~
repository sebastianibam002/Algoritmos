#include <iostream>
#include <string>

bool isPalindrome(string str)
{
  int len = str.length();
  if(len <= 1)
    {
      return true;
    }
  else
    {
      return (str[0] == str[len -1]) && isPalindrome(stdr.substr(1, len -2));
    }
}


int main()
{
  
  return 0;
}
