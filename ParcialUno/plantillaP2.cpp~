#include <iostream>
#include <stack>
#include <queue>
using namespace std;


void invert(queue<int>& Q) {
  /*
   *Su código aquí
   */
}


int main() {
  queue<int> q;
  for(int i = 0; i < 10; ++i){
    q.push(10*i);
    cout << q.back() << " ";
  }
  cout << endl;

  invert(q);

  for(int i = 0; i < 10; ++i){
    cout << q.front() << " ";
    q.pop();
  }
  cout << endl;
  
  return 0;
}
